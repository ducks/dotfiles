[user]
  name = Jake Goldsborough
  email = jake.goldsborough@pm.me

[alias]
  c = commit
  cam = commit -am
  co = checkout
  cob = checkout -b
  moff = merge --no-ff
  dlm = "!git branch --merged main | grep -vE '^\\*|main' | xargs -r git branch -d"

[init]
  defaultBranch = main
